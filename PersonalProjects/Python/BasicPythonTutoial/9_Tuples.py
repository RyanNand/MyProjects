# Tuples are basically lists that CANNOT be modified once created
coordinates = (3, 4)  # Tuples are created with parenthesis while lists use brackets
print(coordinates[1])

locations = [(2, 5), (9, 4), (0, 5)]
print(locations[1])  # Looks like you cannot access values inside tuples in a list of tuples
